// Copia este archivo a `environment.ts` (sin `.example`) y rellena los
// valores reales de tu proyecto Firebase (Configuración del proyecto ->
// Tus apps -> Config del SDK). `environment.ts` está en `.gitignore`: no
// se sube al repo.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: '',
    authDomain: '',
    projectId: '',
    storageBucket: '',
    messagingSenderId: '',
    appId: '',
    measurementId: '',
  },
};
